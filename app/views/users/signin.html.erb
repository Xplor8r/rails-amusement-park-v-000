<%= form_for @user, url: "/users/login" do |f| %>
  <div class="field"><%= f.label :user_name, "Select your name" %><br>
    <%= f.collection_select(:id, @users, :id, :user_name) %>
  </div>
  <div class="actions">
    <%= f.submit "Sign In"  %>
  </div>
<% end %>
