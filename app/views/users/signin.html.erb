<%= form_tag ('/users/login') do %>
  Select your name: <%=collection_select(:user, :name, User.all, :name, :name)%>
  Password: <input name=user[password] type="text" id="password">
  <input type="submit" value='Sign In'>
<% end %>
