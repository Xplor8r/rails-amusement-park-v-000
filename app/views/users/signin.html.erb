<%= form_for @user, url: "/users/login" do |f| %>
    <div class="field"><%= f.label :name, "Select your name" %><br>
      <%= f.collection_select(:name, @users, :name, :name) %>
    </div>
    <div class="actions">
      <%= f.submit "Sign In"  %>
    </div>
  <% end %>
